<?xml version='1.0' encoding='UTF-8'?>
<dataset>
	<FOO
		ID="1"
		NAME="One"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>
	<FOO
		ID="2"
		NAME="Two"
		ANOTHER_ID="${NULL}"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>
	<FOO
		ID="3"
		NAME="Three"
		ANOTHER_ID="2"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>
	<BAR
		ID="1"
		NAME="One"
		FOO_ID="3"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>
	<BAR
		ID="2"
		NAME="Two"
		FOO_ID="3"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>
	<BAR
		ID="3"
		NAME="Three"
		FOO_ID="3"
		CREATED="${SYSTIMESTAMP}"
		UPDATED="${SYSTIMESTAMP}"
		VERSION="1"
	/>

</dataset>
